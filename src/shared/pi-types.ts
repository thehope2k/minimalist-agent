/**
 * Pi sub-provider identifiers — shared across main, preload, and renderer.
 * Single source of truth; do not re-declare in process-local files.
 */
export type PiAuthProvider = 'github-copilot' | 'openai-codex';
